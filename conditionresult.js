console.log(true + true); // 2
// Explanation: true is coerced to 1, so the expression becomes 1 + 1 which results in 2

console.log(true + "true"); // truetrue
// Explanation: true is coerced to "true", so the expression becomes "true" + "true" which results in "truetrue"
